--- a/src/jimm/MainThread.java	2026-09-15 16:05:40.794778937 +0000
+++ b/src/jimm/MainThread.java	2026-09-15 16:07:09.891637879 +0000
@@ -128,6 +128,13 @@
 				int vibraKind = Options.getInt(Options.OPTION_VIBRATOR);
 				if (vibraKind == 2) {
 					vibraKind = SplashCanvas.locked() ? 1 : 0;
+				} else if (vibraKind == 3) {
+					// Bridge build: vibrate only while Jimm is not on screen -
+					// flip closed (with FlipInsensitive the MIDlet keeps
+					// running, only its screen goes away) or another
+					// application in front.
+					Displayable current = Jimm.display.getCurrent();
+					vibraKind = (current == null || !current.isShown()) ? 1 : 0;
 				}
 				if (vibraKind > 0) {
 					Jimm.display.vibrate(500);
--- a/src/jimm/Options.java	2026-09-15 16:05:40.793100536 +0000
+++ b/src/jimm/Options.java	2026-09-15 16:07:09.891503749 +0000
@@ -2397,7 +2397,7 @@
 				Options.OPTION_TYPING_MODE);
 
 		vibratorChoiceGroup = createSelector("vibration", "no" + "|"
-				+ "yes" + "|" + "when_locked", Options.OPTION_VIBRATOR);
+				+ "yes" + "|" + "when_locked" + "|" + "when_hidden", Options.OPTION_VIBRATOR);
 
 		//#sijapp cond.end#
 
--- a/src/lng/RU.lang	2026-09-15 16:05:40.796564756 +0000
+++ b/src/lng/RU.lang	2026-09-15 16:07:09.891767656 +0000
@@ -469,6 +469,7 @@
 "xstatus_rambler_journal"	"Дневник"
 "wait"						"Пожалуйста, ждите ..."
 "when_locked"				"Если заблокирован"
+"when_hidden"				"Если скрыт (крышка закрыта)"
 "warning"					"Предупреждение"
 "wantsyourauth"				" хочет авторизироваться. Причина: "
 "was"						"была"
--- a/src/lng/EN.lang	2026-09-15 16:05:40.799142285 +0000
+++ b/src/lng/EN.lang	2026-09-15 16:07:09.891827040 +0000
@@ -324,6 +324,7 @@
 "volume"					"Volume"  
 "wait"						"Please wait ..."
 "when_locked"				"When locked"
+"when_hidden"				"When hidden (flip closed)"
 "warning"					"Warning"
 "wantsyourauth"				" wants your Authorisation. Reason: "
 "was"						"was"
